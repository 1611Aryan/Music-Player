const data = {
  currentTime: 0,
  duration: 0,
  transformPercentage: 100,
};

const songInfo = (state = data, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default songInfo;
